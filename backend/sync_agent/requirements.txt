# Minimal footprint by design -- this package never imports from `app.*`
# (the FastAPI/Postgres backend) and must be runnable on a bare POS
# terminal that has none of that installed.
httpx==0.27.2
# sqlcipher3 (the maintained package; the older `sqlcipher3-binary`
# distribution only ships manylinux wheels) provides encryption at rest.
# Version 0.6.2 ships prebuilt wheels for Windows/macOS/Linux on py3.11.
sqlcipher3==0.6.2
